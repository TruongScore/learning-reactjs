import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

const Header = props => {
    return (
        <div>
            
        </div>
    );
};

Header.propTypes = {
    
};

export default Header;